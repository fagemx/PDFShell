name: split
description: 將 PDF 檔案分割成多個頁面或頁面範圍。
inputs:
  file:
    type: str
    required: true
  pages:
    type: str
    required: true
    # 例如： "1-3,5,!7" (選擇 1,2,3,5 頁，排除第 7 頁)
  output_dir:
    type: str
    required: false
    default: "." # 預設輸出到當前目錄
