# 📦 .env.sample — PDFShell 專案環境變數範本
# 請複製為 .env 並依實際需求填入

# PostgreSQL 設定
POSTGRES_DB=pdfshell_db
POSTGRES_USER=pdfshell_user
POSTGRES_PASSWORD=pdfshell_pass
POSTGRES_HOST=db
POSTGRES_PORT=5432

# 整合連接字串（建議用於 Django）
DATABASE_URL=postgresql://pdfshell_user:pdfshell_pass@db:5432/pdfshell_db

# Django 設定
DJANGO_SETTINGS_MODULE=pdfshell_srv.settings

# Langchain / OpenAI Agent 使用的 API 金鑰
OPENAI_API_KEY=your_openai_key

# 錯誤通知用 Slack Webhook（選填）
SLACK_WEBHOOK=https://hooks.slack.com/services/XXXX/XXXX
